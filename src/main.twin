twinapp = [
    [
        addQueue('q1', 5)
    ],

    [
        addVar(buffer, ''),
        QueueReceive('q1', buffer),
        printString(buffer)
    ],

    [
        addVar(buffer, 'hello'),
        QueueSend('q1', buffer)
    ]
]
